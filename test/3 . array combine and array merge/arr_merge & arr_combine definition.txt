The array_merge() function merges one or more arrays into one array.

The array_combine() function creates an array by using the elements from one "keys" array and one "values" array and both arrays must have equal number of elements!